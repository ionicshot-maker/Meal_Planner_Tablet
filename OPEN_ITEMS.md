# Angelo Family Meal Planner — Open Items

*Last updated 2026-07-24 (post-fix pass — barcode consolidation, atomic reset, ReviewScreen auto-merge, atomic mergeIngredients(); ReviewScreen fix and mergeIngredients() atomicity fix both live-verified). This is a living checklist the user maintains directly — edit it freely.*

**Active bugs, ranked by severity** *(full risk analysis — call chains, concrete triggers, verified examples against real data — lives in the 2026-07-24 audit conversation, not repeated here)*

*Data Integrity/Consistency Risk:*
- [ ] `resolveIngredientDuplicate()`/`resolveRecipeDuplicate()` (`db/supabase.ts`, Cloud Sync's duplicate-resolution path) never repoints `variantId`, grocery lists, or macro logs — only `Recipe.ingredients[].ingredientId`. Same incompleteness class `mergeIngredients()` was built to fix, never backported here (previously noted in `MealPlannerApp_Reference.md`'s 2.9 changelog entry, tracked here now too).

*Functional Risk (batched — common root cause: missing error handling around sequential per-item write loops, meaning a mid-loop failure can hang the UI in a stuck "saving/importing" state with no visible error, though already-saved items remain individually valid and safe to retry):*
- [ ] No try/catch around the save loop in: `JsonImportTab.tsx handleImport()`, `BulkEntryTab.tsx handleSaveAll()`, `StarterLibraryPrompt.tsx`'s silent auto-seed effect (empty-install/legacy-migration paths — can strand the "Updating ingredient library…" toast), `DataSection.tsx executeImport()` (has an outer catch, but the generic "Import failed" message can understate partial success since some of its 7 sequential per-store transactions may have already committed). Also in this bucket: Receipt Scanner's "High confidence" badge doesn't distinguish an exact name match from a lone fuzzy match, which can overstate certainty even though a human review step exists before commit.

**Security**
- [ ] Rotate Supabase service_role key (deferred while active dev work continues — do before any real household member other than me starts using the app)
- [ ] Run Phase 3 (close legacy household_code bridge) — unblocked now that I'm confirmed real owner

**Data cleanup**
- [ ] Dill Pickle sodium conflict (290mg vs 900mg) — needs a real label check
- [ ] ~31 remaining messy Great Value brand strings (Organic/Walmart/Bakery variants) not yet normalized
- [ ] Reconcile true current ingredient count (has drifted across sessions: 5,698 → 6,125 → 6,121 → 6,129)

**Original pending list**
- [ ] Export Cookbook + Ingredients for bulk recipe-ingredient linking

**Feature gaps (known, undecided)**
- [ ] TDEE/BMR calculator for Macro Tracker
- [ ] Unify the 3 separate ingredient-matching implementations (Receipt Scanner / JSON Import / branded pack) — **barcode comparison specifically was unified 2026-07-24** (see Recently Completed): `receiptMatching.ts` now imports `findAllBarcodeMatches`/`findMatchingVariant` from `smartDuplicate.ts` instead of its own bespoke checksum/lookup logic, so that part of the "3 independent implementations" finding no longer applies. What's still unaddressed: fuzzy *name*-matching remains 3 separate call sites with different tuning (JSON Import/branded pack use `findSmartMatches` with an `{ fuzzy: false }` override for exact-only behavior; Receipt Scanner's `matchLine()` has its own confidence-tier/candidate-ranking logic layered on top of `findSmartMatches`) — that consolidation is still open.

**New ideas, not yet scoped**
- [ ] Bill due-date awareness relative to payday (explicitly NOT a budget/amount tracker — just "here's what's due before your next payday," reusing existing payday-schedule infrastructure)
- [ ] Standalone PLU lookup capability for Receipt Scanner produce items — a static IFPS PLU→generic-produce-name table (e.g. `4038` → Raspberries, `4048` → Bananas), no API/network call/schema change needed (resolves to a name/category hint, not a `variant.barcode` match). Found while verifying why Blueberries/Raspberries on a real receipt only got NAME MATCH suggestions despite numeric codes being present: confirmed **by design, but not for this reason** — `isValidBarcodeChecksum()` (`src/utils/barcodeValidation.ts`) hard-rejects anything that isn't exactly 12/13 digits, so a real 4-5 digit PLU code is rejected before ever reaching ingredient lookup; separately, the Gemini extraction prompt (`netlify/functions/gemini-receipt-scan.ts`) describes the `barcodeText` field as "UPC/PLU, typically 11-13 digits" — internally inconsistent, since real PLU codes are 4-5 digits, not 11-13. No PLU-handling logic exists anywhere in the codebase today (confirmed via full-repo search). Not a bug — current behavior is consistent with the existing UPC/EAN-only design — but a real, unbuilt capability gap for a receipt category (produce) that's extremely common.

**Recently completed (for reference, remove once confirmed no follow-up needed)**
- [x] Made `mergeIngredients()` (`db/mergeIngredients.ts`) genuinely atomic — rewrote from 5 separate sequential writes (recipe repointing, grocery-list repointing, macro-log repointing, category reconciliation, ingredient deletion) into one shared `db.transaction(['ingredients','recipes','groceryLists','macroLogs'], 'readwrite')`, reusing the exact pattern already established by `applyReceiptSaveBatch()` and `performReset()` rather than inventing a third transaction approach. All four affected stores live in the same IndexedDB database, so full atomicity was directly achievable — no store had to be left out. Closes the specific gap the audit flagged: an interruption after deletion but before category reconciliation used to leave the category permanently unreconciled (retrying threw immediately since the merge-away ingredient no longer existed); now deletion and reconciliation are part of the same atomic unit, so that state can't happen. Also found and fixed, while writing the transaction-rollback verification script: a real (if minor) unhandled-promise-rejection edge case — when a plain validation error (e.g. "ingredient not found") throws before `tx.done` is ever awaited, the manual `tx.abort()` in the catch block rejects `tx.done` with nothing listening; added `tx.done.catch(() => {})` right after the abort call to swallow it safely. The same latent characteristic exists unpatched in `applyReceiptSaveBatch()` (out of scope for this fix). Callers (`MergeIngredientModal.tsx`, `DevToolsPage.tsx`) needed no changes — both already display `e.message` on failure with a generic "Merge failed — nothing was changed." fallback, which is now literally accurate rather than potentially misleading. **Verified:** code-reviewed the diff directly. Empirically verified (not just reasoned about) using a real spec-compliant IndexedDB implementation (`fake-indexeddb`, installed temporarily via `--no-save` and removed afterward — never added to `package.json`) driven through the same `idb` wrapper the app uses: ran the exact new transaction logic to completion (happy path, all 4 stores correctly updated) and, separately, forced failures at 4 different points — after recipe repointing, after grocery-list repointing, after macro-log repointing, and immediately after the final delete — confirming in every case that ALL earlier writes in that same transaction (including ones that had "succeeded" moments before the forced failure) rolled back completely, with the database left byte-identical to its pre-merge state — 20/20 checks passed across the happy path plus all 4 failure points. Build-verified; deployed; confirmed live via byte-identical MD5 hashes of all 3 local build chunks containing the merge code (main bundle, `IngredientsPage-*.js`, `DevToolsPage-*.js`) against the fetched production chunks. **Click-verified live in production** (not just logic-verified, unlike the prior two fixes in this pass): created two throwaway test ingredients ("ZZZ TEST MERGE KEEP" / Meat & Poultry, "ZZZ TEST MERGE AWAY" / Soups & Broths) directly in the live household database via the real Ingredients UI, used the real "Merge…" button end-to-end including the category-disagreement picker (chose "Soups & Broths" to actually exercise reconciliation, not just accept the default), confirmed "Merge Complete" showed the correct 0/0/0 reference counts (both test ingredients had no real references), confirmed live that MERGE AWAY was gone and MERGE KEEP survived with its category correctly changed to the picked value ("Soups & Broths", not its original "Meat & Poultry") — proving category reconciliation itself works live, not just in the Node script. Cleaned up afterward: deleted the surviving test ingredient; a full page reload confirmed the household's ingredient count returned to exactly 6,246, its pre-test value, with zero residue in the real data. Supersedes the "Not click-verified live" caveat on the category-reconciliation entry below — that gap is now closed.
- [x] Fixed ReviewScreen.tsx's silent single-fuzzy-match auto-merge — `handleSave()` no longer commits a variant merge automatically when `findSmartMatches()` returns exactly one match; it now unconditionally shows the "Similar Ingredient Found" modal (renamed action button: "Merge — Add as Variant of \"X\"") for any match count ≥ 1, requiring an explicit user click. Modal enhanced with a new `IngredientCompareCard` (name, brand, category, macros, barcode) shown for both the draft and each existing match side-by-side, reusing MergeIngredientModal's comparison-card visual pattern rather than building a new one. Also replaced the bare `catch { setSaving(false) }` in both `handleSave()` and `handleSaveAsNew()` with a visible error message (`saveError` state, rendered above the footer). **Verified:** code-reviewed directly (the only remaining code path for matches.length > 0 is `setSmartMatches(...)`, confirmed by reading the full function — no branch left that calls `addAsVariantOf` without a user click); logic-verified that the real audit scenario ("Turkey Bacon" fuzzy-matching "Bacon" via the one-directional keyword-subset check) still produces exactly one match against a faithful copy of `findSmartMatches`, proving the fix's confirmation path is the one actually triggered for this case. Build-verified; deployed; confirmed live via byte-identical MD5 hash match between the local build's chunk (`PhotoCaptureCrop-*.js`, where ReviewScreen's code landed after code-splitting) and the fetched production chunk. **Click-verified live 2026-07-24 by the user directly**: searched "Ramen Noodles" in production; the confirmation modal correctly appeared showing several fuzzy candidates with the new side-by-side compare cards, none of which were actually the same product; user chose Save as New and no silent merge occurred. Confirms the modal-based no-silent-merge mechanism and the new compare-card UI both work correctly end-to-end against real data. This particular search returned multiple candidates (the ≥2-match case, which already showed a modal before this fix); the exact single-match branch that was the original bug trigger ("Turkey Bacon" → "Bacon") shares the identical code path (`matches.length > 0` → `setSmartMatches(...)`, no branch skips it) but wasn't itself the specific case exercised by this live test.
- [x] Consolidated barcode comparison onto one shared implementation — `findBarcodeMatch()`/`findAllBarcodeMatches()`/`findMatchingVariant()` in `smartDuplicate.ts` are now the single authoritative barcode-matching path for the whole app (two-tier: exact match first, then checksum-valid UPC-A/EAN-13 leading-zero-equivalent candidates via the existing `barcodeValidation.ts` pipeline). `findIngredientByBarcode()` (`db/ingredients.ts`), `receiptMatching.ts`'s `matchLine()`, and `ReviewScreen.tsx`'s inline barcode check all now call the shared functions instead of doing their own plain `===` comparison or (for Receipt Scanner) a separate private implementation. **Verified:** code-reviewed all 4 call sites' diffs directly. Logic-verified with a Node script reproducing the exact new implementation against: a real checksum-valid barcode pair from this session's earlier merge work (leading-zero-stripped vs. full form) now matching in both directions; exact-match backward compatibility preserved for a checksum-invalid stored value (zero regression); a checksum-invalid query correctly not spuriously matching anything; `findAllBarcodeMatches` correctly preferring the exact-match tier over the candidate tier when both exist — 6/6 checks passed. Build-verified; deployed; confirmed live via byte-identical MD5 hash of the local build's main bundle (where this shared logic lives) against the fetched production bundle. **Not click-verified live** — no scan/lookup/manual-entry workflow was driven through an actual running browser this session.
- [x] Made `DataSection.tsx performReset()` genuinely atomic — rewrote from N independent sequential `db.clear(store)` calls (no error handling at all) to a single shared `db.transaction(stores, 'readwrite')` wrapping every store's `.clear()`, so a failure partway through rolls back everything in that transaction automatically rather than leaving some stores cleared and others untouched. On failure, a clear user-visible error message is now shown (`resetError` state, reusing the existing result/error modal) stating that nothing was changed; the Reset/Cancel buttons are disabled and the Reset button shows "Resetting…" while in progress, so the UI can no longer look silently stuck. **This is real atomicity, not just better error reporting** — confirmed by the empirical test below. **Verified:** code-reviewed the diff directly. Empirically verified (not just reasoned about) using a real spec-compliant IndexedDB implementation (`fake-indexeddb`, installed temporarily and removed afterward — never added to `package.json`) driven through the same `idb` wrapper library the app uses: ran the exact new transaction code to completion (both stores end up empty) and, separately, forced a mid-transaction abort after the first store's `.clear()` had already run — confirmed that store's data was fully rolled back and left byte-identical to its pre-reset state, proving the "all-or-nothing" claim rather than just asserting it — 5/5 checks passed. Build-verified; deployed; confirmed live via byte-identical MD5 hash of the local build's `SettingsPage-*.js` chunk against the fetched production chunk, and by confirming the distinctive `"all-or-nothing"` error-message string is present in that live chunk. **Not click-verified live** — the Reset confirmation flow was never actually clicked through in a running browser this session.
- [x] Imported USDA comprehensive JSON (366 items) — re-run after the fuzzy-matching fix below. Confirmed correct: 124 added, 0 updated, 242 skipped — matches the expected 124-new/242-existing split exactly.
- [x] Fixed JSON Import's near-100% false-positive duplicate detection — `findIngredientMatch()` was called with no options, defaulting fuzzy matching ON. `findSmartMatches()`'s keyword-subset check matches any single/two-word name against *any* existing name merely containing those words (e.g. "Lime" matched "Cilantro Lime Rice"), and its edit-distance check matched unrelated short words by coincidence ("Lime" vs "Rice", distance 2). Confirmed live: Add New Only mode on the 366-item USDA file reported 0 added, 366 skipped, when only 242 were genuine duplicates. Traced the exact logic against a representative mock catalog — all 7 sampled "new" names (Lime, Watermelon, Carrot, Onion Yellow, Potato Red, Shallot, Bell Pepper Green) fired a spurious match, each with the specific existing name and mechanism (keyword-subset vs. edit-distance) identified. Same root cause `brandedLibrary.ts` already fixed with `{ fuzzy: false }` — just never applied to this caller. Fixed the same way. Verified against mock data reproducing the exact failure pattern (0/7 added under old behavior, 4/7 correctly added under the fix, all genuine duplicates still correctly skipped) — 5/5 checks passed. Deployed; confirmed live via byte-identical hash match between the local build and the fetched production bundle.
- [x] Fixed atomic-transaction gap on Receipt Scanner's household-item writes — `applyIngredientBatch()` (ingredients-only transaction) replaced with `applyReceiptSaveBatch()`, one IndexedDB transaction spanning `ingredients` + `householdItems` + `processedReceipts`. Build-verified; deployed to production and confirmed live by fetching the exact production bundle and grepping for the new transaction code. Not click-verified live (no browser access this session) — logic mirrors the already-working single-store version, just extended to 3 stores (a standard IndexedDB guarantee, not new behavior to prove out).
- [x] Fixed mergeIngredients() category reconciliation — added an optional `{ category }` param; when the two merging ingredients disagree on category, both merge UIs (Barcode Duplicate Finder, Ingredients page) now show a simple dropdown to pick which one survives, instead of always silently keeping whichever the "keep" record happened to have. Build-verified, logic-verified against mock data (4/4 checks incl. back-compat with no option passed), deployed and confirmed live via bundle fetch. *(Update 2026-07-24: since click-verified live end-to-end as part of the mergeIngredients() atomicity fix above — the category-disagreement picker was actually used against real production data and confirmed to change the surviving ingredient's category correctly.)*
- [x] Overnight Claude-for-Chrome price lookup — status checked, confirmed done
- [x] "Add at least one person profile" setup step — confirmed complete
- [x] Ingredient Converter v5 — fixed leading-zero barcode stripping at both the row-parsing and pandas dtype-inference layers
- [x] Barcode Duplicate Finder built in Dev Tools, live-verified against real data
- [x] Merge/Keep/Skip/Delete actions for duplicate ingredients — general mergeIngredients() capability, verified with a real positive-repoint test (1 recipe successfully repointed) and a real negative test (unrelated ingredient correctly returned 0/0/0)
- [x] Fixed a real gap found during this work: grocery list references were never repointed by any prior merge/dedup logic — now fixed
- [x] Dev Tools access control — fixed case-sensitivity bug in owner detection, then removed an over-broad fallback that reopened a security hole
- [x] All 8 leading-zero barcode duplicate pairs resolved (5 merged interactively, final 3 documented in TEST_FIXTURES.md then merged manually)
